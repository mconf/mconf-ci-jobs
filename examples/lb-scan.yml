name: Security Scan

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  scan:
    uses: mconf/mconf-ci-jobs/.github/workflows/lb-scan.yml@main
    # with:
    #   trivy-debug: false # (optional) Enable Trivy debug output to diagnose slow or failing scans (default: false)
    secrets: inherit

